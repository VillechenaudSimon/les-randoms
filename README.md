
# les-randoms

## Running Locally

1. Fill the local-run.sh script depending on the database connection string
2. In Git Bash :
```sh
$ ./local-run.sh
```

## Deploying to Heroku

In Windows CMD :
```sh
$ git push heroku main
```

## View Remote Logs

In Windows CMD :
```sh
$ heroku logs --tail
```